# Tasks

## 1. 自测骨架、内核适配与构建真源

- [x] 1.1 新建 `test/mini/stub/`，提供 host 专用头文件：`securec.h`（本组件用到的 `memset_s`/`memcpy_s`/`strcpy_s`/`strncpy_s` 等内联实现，越界时返回错误）、`telephony_errors.h`（逐字节拷贝上游 core_service 60b603e，来源登记在 1.9 的 `vendor_manifest.json`）、`parameter.h`（host 版 `GetParameter`，返回缺省值），并核对关键错误码与上游取值一致（由 1.9 的 `vendor_files.py check` 执行）。验证：核对一致时返回 0；人为改动 stub 中任一枚举时返回非 0
- [x] 1.2 新增 `utils/mini/include/tel_os_adapter.h` 和 `utils/mini/src/tel_os_adapter_posix.cpp`：互斥锁、计数信号量（带超时）、单次软件定时器、任务、当前任务 ID、让出 CPU、毫秒转 Tick（向上取整、64 位中间值、结果限制在“永久等待”特殊值之下）、当前 Tick，以及“已创建内核对象计数”查询。验证：1.4 的自测覆盖“信号量超时返回超时错误”“定时器到期回调恰好一次”“1 ms 在 100 Hz 下取 1 Tick”“超大毫秒值不回绕”
- [x] 1.3 在 `utils/mini/include`（必要时加 `utils/mini/src`）中实现基础替身：`refbase.h`（原子强/弱计数，`sptr`/`wptr`/`MakeSptr`）、`singleton.h`（`DelayedSingleton`/`DelayedRefSingleton`/`Singleton`，原子一次构造，等待方经 1.2 让出 CPU）、`errors.h`（逐字节拷贝上游 c_utils 3bdc11e，来源登记在 `vendor_manifest.json`）、`iremote_broker.h`、`nocopyable.h`、`pac_map.h`（条目上限 `CALL_MANAGER_MINI_PACMAP_MAX_ENTRIES`）、`want_params_wrapper.h`、`string_ex.h`、`ffrt.h`（主循环限定的无内核对象类型）、`telephony_log_wrapper.h`（运行时去掉 `{public}`/`{private}`，默认 printf 后端），以及 `call_manager_mini_config.h`（常量缺省值加 `static_assert`，例如栈大小不超过 `uint16` 上限）。这些头文件都先 include design D11 中列出的 std 头。验证：1.4 的自测覆盖“多任务并发增减引用，最后一个引用销毁对象”“并发首次访问单例只构造一次”“PacMap 读取缺省值、类型读写、超上限时 Put 失败”“`%{public}d` 的输出与 `%d` 一致”
- [x] 1.4 新增 `test/mini/mini_selftest.cpp`（单一翻译单元；内含最小用例框架，统计通过/失败数并以返回码表示结果；用例放在 `test/mini/cases/*.inc` 中由它 include）和 `test/mini/mini_selftest.sh`（源文件集合、include 顺序、宏和选项与 `run_mini_cmsis.sh` 一致，内核适配换成 POSIX 版，排除 `tel_os_adapter_cmsis.cpp` 和 `call_manager_service_lite.cpp`）。验证：`bash test/mini/mini_selftest.sh` 以 `-Werror` 编译并运行 1.2/1.3 的用例，全部通过，连续两次结果一致
- [x] 1.5 搭建让 `call_manager_client.cpp` 原样编译所需的骨架：`utils/mini/include/iremote_broker.h`（无 IPC 能力的本地接口基类与描述符宏，让同目录被 `call_manager_client.h` 包含的 `i_call_status_callback.h`、`transfer_control.h` 原样编译；引号 include 总是先查包含者所在目录，因此这两个头无法用 Mini 同名头替换）、`frameworks/native/include/mini/call_manager_proxy.h`（声明 `call_manager_client.cpp` 调用到的全部方法），以及 `frameworks/native/src/mini/call_manager_proxy.cpp` 骨架：范围外方法按 `specs/mini-call-client-api` 返回，范围内方法在接入服务前返回 `TELEPHONY_ERR_UNINIT`。验证：`vendor_files.py check` 确认 `call_manager_client.cpp` 与标准仓来源逐字节一致；两个门禁脚本都能零告警编译；自测覆盖“未调用 `Init` 时 `HangUpCall` 返回 `TELEPHONY_ERR_UNINIT`”“`CombineConference` 返回 `CALL_ERR_FUNCTION_NOT_SUPPORTED`”“`RegisterBluetoothCallManagerCallbackPtr` 返回空指针”
- [x] 1.6 新增 `utils/mini/src/tel_os_adapter_cmsis.cpp`：CMSIS-RTOS2 的 `osMutex*`/`osSemaphore*`/`osTimer*`/`osKernelGetTickCount`/`osKernelGetTickFreq`/`osThreadGetId`，任务使用 `LOS_TaskCreate`；它与 POSIX 版定义同一组符号。验证：`CALL_MANAGER_ROOT=<组件路径> bash run_mini_cmsis.sh` 在默认内核配置下编译无告警，1.2 的用例全部通过；把两套适配同时加入一次编译时，链接因重复定义而失败
- [x] 1.7 在 Mini 仓根目录新增 `callmanager_mini.gni`（部件路径 `//base/telephony/call_manager_mini`）：`call_manager_mini_sources`、`call_manager_mini_include_dirs`（顺序与门禁一致，外部依赖目录排在最后）、`call_manager_mini_defines`（`TELEPHONY_MINI_SYSTEM`，以及由 `declare_args()` 转换来的常量和权限配置）、`call_manager_mini_cflags_cc`。同时新增 `test/mini/tools/check_mini_layout.py`：检查 gni、`mini_selftest.sh`、`run_mini_cmsis.sh` 三处 include 顺序一致，以及 gni 源文件表与仓内实际源文件一一对应。验证：脚本通过；人为调换 gni 中两个 include 目录或漏列一个 Mini 源文件时，脚本返回非 0；telephony_call_manager 仓的 `git status` 为空
- [x] 1.8 新增 `test/mini/tools/check_header_origin.py`：用 `g++ -H` 编译自测的全部翻译单元，确认所有存在 Mini 版的头文件都从 Mini 目录引入，所有头文件都来自 Mini 仓自身或声明的外部依赖（不引用 telephony_call_manager 仓，不出现 c_utils 的 include 目录），`errors.h` 来自 `utils/mini/include`。验证：脚本通过；人为在 include 路径前插入 telephony_call_manager 的 `interfaces/innerkits` 时，脚本报错
- [x] 1.9 新建独立仓 `telephony_call_manager_mini`（本地 `git init`，不推送远端），按标准仓的相对目录布局放置 Mini 文件；把原样复用的标准文件（10 个 innerkits 头、`frameworks/native/src/call_manager_client.cpp`、`LICENSE`）从标准仓 24e1785f 逐字节拷入，并与上游拷贝（`telephony_errors.h`、`errors.h`）一起登记到 `vendor_manifest.json`；新增 `test/mini/tools/vendor_files.py`（`check` 核对拷贝与来源逐字节一致及错误码取值，`sync` 从来源 HEAD 刷新并更新登记的提交）。验证：`vendor_files.py check` 返回 0；人为改动任一拷贝文件时返回非 0；telephony_call_manager 仓的 `git status` 为空；两个门禁在新仓上通过

## 2. 事件处理器与权限适配

- [x] 2.1 实现 `utils/mini/{include,src}/los_event_handler.*` 的异步部分：`PostAsyncTask`（立即或延迟）、`RemoveAsyncTask`、延迟表（容量 `CALL_MANAGER_MINI_MAX_DELAYED_TASKS`，按到期时间排序，共用 1 个单次定时器，定时器回调只投递 `TIMER_FIRE`）。验证：自测覆盖 `specs/mini-call-runtime` 中“到期前撤销”“已出队后撤销”两个场景，以及“延迟表满时投递失败，由投递方释放负载”
- [x] 2.2 实现 `PostSyncTask`：上下文放在堆上并由双方共享引用计数，等待者信号量池大小为 `CALL_MANAGER_MINI_MAX_SYNC_WAITERS`，超时后标记为已放弃，主循环只对未放弃的请求释放信号量，在主循环内直接执行。验证：自测覆盖“阻塞主循环超过上限时，调用方按时返回 `TELEPHONY_ERR_IPC_CONNECT_STUB_FAIL`，任务随后完成且不写入调用方内存”“超时后同一等待槽的下一次调用不被误唤醒”“等待者超上限时立即返回错误”“主循环内同步调用直接执行”
- [x] 2.3 实现两种驱动模式：自驱动模式（环形队列容量 `CALL_MANAGER_MINI_QUEUE_SIZE`，1 个循环任务）和外部驱动模式（`Dispatch` 加可注入的投递函数，供 samgr 绑定使用），以及 `BindLoopTask`/`IsOnLoop`。验证：自测覆盖“队列满时投递立即返回错误、负载被回收”“其他任务的投递按 FIFO 执行”“外部驱动模式下用测试投递函数完成一次同步调用”
- [x] 2.4 实现退出与初始化回滚：退出顺序为“原子停止标志 → 停止定时器 → 等待在途任务 → 回收剩余消息 → 删除内核对象”，支持重复退出；初始化时任一内核对象创建失败，就释放已创建的对象并返回失败。验证：自测覆盖“退出期间投递返回 `TELEPHONY_ERR_UNINIT`”“二次退出安全”；以 `SEM_LIMIT=1 MINI_TEST_FILTER=InitFailureRollsBack bash run_mini_cmsis.sh` 运行时（harness 不向自测传参，过滤条件经环境变量传入；1 个信号量的池子装不下其余用例），“对象池耗尽导致初始化失败并完整回滚”用例通过；该用例在默认对象池下还依次耗尽互斥锁池和定时器池，确认每种对象创建失败时都完整回滚
- [x] 2.5 编写内核对象预算用例：借助 1.2 的计数接口，统计事件处理器在两种驱动模式下创建的互斥锁、信号量、定时器和任务数量。验证：统计结果与 design“资源所有权与预算”中的表一致；CMSIS 门禁在默认对象池下通过
- [x] 2.6 实现 `utils/mini/{include,src}/call_permission_adapter.*`：`CheckPermission`/`IsSystemCaller`/`GetCallerBundleName`，由 `call_manager_mini_granted_permissions`、`call_manager_mini_caller_is_system_app`、`call_manager_mini_caller_bundle_name` 转成的 defines 驱动，默认拒绝。验证：自测覆盖“默认配置全部拒绝”和“以 `-D` 注入授予 `PLACE_CALL` 后仅该权限通过”（后者由 `mini_selftest.sh` 以 gni 生成的 define 形式单独编译 `test/mini/probe/permission_macro_probe.cpp` 两次执行，分别覆盖未声明和已声明系统应用两种信任模型）

## 3. 注册表接口与上行入口

- [x] 3.1 新增 `interfaces/innerkits/mini/cellular_call_lite_interface.h`（签名沿用 `CellularCallInterface` 中的范围内方法，不含 `IRemoteBroker`/`Surface`）和 `utils/mini/src/` 中的注册表：注册、注销，就绪通知在锁内快照、锁外分发。验证：自测覆盖“注册后订阅者收到一次就绪通知”“未注册时查询返回空”（注册表用原子自旋锁保护，见 design D8）
- [x] 3.2 新增 `interfaces/innerkits/mini/core_service_lite_interface.h`、对应注册表，以及 Mini 同名 `CoreServiceClient`（`services/telephony_interaction/{include,src}/mini`），覆盖移植代码中用到的 SIM/网络查询。验证：自测覆盖已注册和未注册两种情况下的返回值
- [x] 3.3 实现 `CallStatusCallbackGuard`（`services/telephony_interaction/{include,src}/mini`）：它实现 Mini `ICallStatusCallback`，校验卡槽号、枚举值、号码 NUL 结尾且长度不超过 255、批量条目不超过 5，校验通过后按值拷贝并投递到主循环。验证：自测覆盖 `specs/mini-call-state-reporting` 中“批量上报条目超限”“号码未以 NUL 结尾”两个场景，确认状态不变且日志不含号码

## 4. 通话业务移植（`services/call/src/mini` 等）

- [x] 4.1 移植通话对象：用 `vendor_files.py` 的清单从标准仓拷入并登记本组要复用的 `services/call/include` 头（`call_base.h`、`carrier_call.h`、`cs_call.h`、`conference_base.h`、`cs_conference.h`、`ims_conference.h`、`common_type.h` 等，以及后续 4.x 用到的 `call_object_manager.h`、`call_state_listener*.h`、`call_status_policy.h`、`call_request_process.h`）；新增 Mini 版 `ims_call.h`；在 `src/mini/` 中实现 `call_base.cpp`、`carrier_call.cpp`、`cs_call.cpp`、`ims_call.cpp`、`conference_base.cpp`、`cs_conference.cpp`、`ims_conference.cpp`，复用头中声明的虚函数全部给出实现，视频类返回 `CALL_ERR_VIDEO_NOT_SUPPORTED`。验证：自测覆盖通话属性读写、状态派生（`TelCallState` → `CallRunningState`），以及 `specs/mini-feature-scope-build` 中“复用头文件中声明的视频方法”场景
- [x] 4.2 移植 `call_object_manager.cpp`（复用标准头），并在添加对象处实现容量上限 `CALL_MANAGER_MINI_MAX_CALL_OBJECTS`（1 个槽位保留给 ECC）。验证：自测覆盖按 callId、按 index 加 slotId 查找，移除后返回空指针，以及 `specs/mini-voice-call-control` 中“普通槽位已满”“普通槽位已满时拨打紧急号码”两个场景
- [x] 4.3 移植 `call_state_listener.cpp` 和 `call_status_policy.cpp`（复用标准头）。验证：自测覆盖“每次迁移向每个订阅者广播恰好一次”，以及 host 下主循环限定断言（在非主循环任务中加锁会触发断言）
- [x] 4.4 实现 Mini 同名 `CellularCallConnection`（`services/telephony_interaction/{include,src}/mini`）：范围内方法转发到 3.1 的注册表，未注册时返回 `TELEPHONY_ERR_IPC_CONNECT_STUB_FAIL`，范围外方法返回 `CALL_ERR_FUNCTION_NOT_SUPPORTED`，底层错误码原样返回；收到就绪通知后把“注册 Guard”投递到主循环，并幂等地只执行一次。验证：自测用协议栈测试替身覆盖上述行为，以及 `specs/mini-call-runtime` 中“协议栈晚于通话管理就绪”“协议栈已就绪时重复触发注册”两个场景
- [x] 4.5 移植上行链路：Mini `call_status_callback.*`、`report_call_info_handler.*`（在主循环内直接处理）、`call_status_manager.*`（Mini 同名头；包括 `HandleCallReportInfo`/`HandleCallsReportInfo`/`HandleDisconnectedCause`，以及 Incoming/Dialing/Alert/Active/Holding/Waiting/Disconnecting/Disconnected 各 Handle 函数和 `UpdateCallState`）。来电不经拦截；表满时通过协议栈拒接。验证：自测用协议栈替身上报 DIALING → ALERTING → ACTIVE → DISCONNECTED，确认回调顺序正确、对象表最终为空；覆盖“对象表已满时来电”“延迟任务执行前通话已移除”“两次通话之间无状态泄漏”三个场景
- [x] 4.6 移植 Mini `call_number_utils.*`（去分隔符、紧急号码判定经协议栈适配、卡槽校验；格式化、归属地、黄页返回 `CALL_ERR_FUNCTION_NOT_SUPPORTED`）和 `call_policy.*`（Mini 同名头；各动作的 Policy 函数与 `CheckCallLimit`，超级隐私/卫星/EDM 判定按“未开启”处理，ECC 按标准豁免）。验证：自测覆盖“空号码”“策略拒绝”“裁剪能力被省略时拨打紧急号码”“通话数已达上限时拨打紧急号码”
- [x] 4.7 移植下行链路：Mini `call_request_event_handler_helper.*`（拨号处理标志与超时任务改用 `LosEventHandler`）、`call_request_handler.*`（投递失败返回 `TELEPHONY_ERR_IPC_CONNECT_STUB_FAIL`）、`call_request_process.*`（`HandleDialFail` 不做等待）、`call_control_manager.*`（Mini 同名头；拨号、接听、拒接、挂断、保持、解除保持、切换、查询类方法，以及 `CallStateObserve` 只注册保留的订阅者）。验证：自测覆盖“连续两次拨号”“协议栈拒绝拨号”“失败处理不自等待”“挂断不存在的通话”“接听来电（先 `ANSWERED` 再 `ACTIVE`）”“保持后切换”“请求卫星通话”“协议栈适配未注册”“队列满时 `HangUpCall` 返回错误且状态不变”
- [x] 4.8 执行高危序列用例：正常拨号；拨号失败后再次拨号；拨号时队列中已有他路上报；来电 → 接听；来电 → 拒接；保持 → 切换；通话中第二路来电；紧急呼叫（含普通槽位满）；最后一路结束后状态归零。验证：全部用例在两个门禁下通过，每个用例在 `test/mini/cases/` 中标注所对应的 spec 场景名

## 5. 服务入口、客户端与回调

- [x] 5.1 实现 Mini 同名 `CallAbilityReportProxy`（`services/call_report/{include,src}/mini`）：单回调注册表在主循环中修改；状态广播时在主循环中取快照，逐个异步投递；`CallAttributeInfo`/`DisconnectedDetails`/`CallEventInfo` 按值拷贝；回调返回失败不影响状态机。验证：自测覆盖“来电上报”“回调返回错误”两个场景，以及同一通话的回调顺序
- [x] 5.2 实现 Mini 同名 `CallManagerService`（`services/call_manager_service/{include,src}/mini`）：design D6 表中的判定点和权限组合、`DialCall` 白名单重建、号码长度校验、`bundleName` 来源、`RejectCall(isSendSms=true)` 与 `AnswerCall(非语音)` 的前置拒绝，以及按 D1 顺序执行的 `Init`（逆序回滚、身份设置后才置为就绪）、`UnInit` 和退出。验证：自测覆盖 `specs/mini-call-client-api` 中“调用方注入非白名单字段”“调用方伪造 bundleName”“号码超长”“拒接并发送短信”“以视频方式接听”“未配置信任模型”“已配置信任模型”，以及 `specs/mini-call-runtime` 中“启动早期调用”“中途失败”“重复初始化”“退出期间有请求到达”“重复退出”
- [x] 5.3 完成 Mini `CallManagerProxy` 的接线，以及 Mini `CallAbilityCallback`（由 `sptr` 持有，带关闭标志）：范围内方法把参数按值拷贝后经 `PostSyncTask` 调用服务；`RegisterCallBack` 保持单回调语义并拒绝空回调；`HasVoiceCapability` 经 `parameter.h` 读取。验证：自测覆盖“正常拨号返回”“错误码与标准侧一致”“重复注册”“注销后无回调”“服务处理超时”
- [x] 5.4 实现 samgr 绑定 `services/call_manager_service/src/mini/call_manager_service_lite.cpp`：`GetName` 返回 `"call_manager"`；`Initialize` 中先 `BindLoopTask` 再执行 `Init`；`MessageHandle` 调用 `Dispatch`；`GetTaskConfig` 返回 `SINGLE_TASK` 并使用资源常量；投递使用 `SAMGR_SendRequest`（`len = 0`，失败时由投递方释放）；通过默认 Feature 注册本地 API；`SYS_SERVICE_INIT` 注册。验证：用 `.deps/samgr_lite` 的上游头文件和 harness 的 LiteOS 桩，对该文件执行 `g++ -fsyntax-only -Wall -Wextra -Werror`，零告警（`mini_selftest.sh` 在检测到 `.deps/samgr_lite`、`.deps/utils_lite` 与 harness LiteOS 桩时自动执行该检查；utils_lite 取自上游 master 1e88eb4d，提供 `ohos_init.h`/`ohos_errno.h`）
- [x] 5.5 编写端到端自测：在 `OnCallDetailsChange` 回调内调用 `AnswerCall`；从非主循环任务发起 `DialCall` 并等待结果；多路通话过程中内核对象计数保持不变。验证：三个用例在两个门禁下都通过，不死锁；内核对象计数与 design 表一致（覆盖 `specs/mini-call-runtime` 中“多路通话不增加内核对象”场景）

## 6. 音频降级

- [x] 6.1 新增 `interfaces/innerkits/mini/call_audio_lite_interface.h` 及对应注册表，以及 Mini 同名 `AudioControlManager`（`services/audio/{include,src}/mini`），按 design D10 的状态映射实现；未注册实现时返回 `CALL_ERR_FUNCTION_NOT_SUPPORTED`，只记录一次日志。验证：自测覆盖 `specs/mini-platform-adaptation` 中“无音频实现时来电”“通话接通”两个场景，以及最后一路结束后释放通话音频通路

## 7. 构建描述与收尾检查

- [x] 7.1 在 Mini 仓根目录新增 `BUILD.gn`（`import("//build/lite/config/component/lite_component.gni")`，`ohos_kernel_type == "liteos_m"` 时定义 `static_library("tel_call_manager_mini")`，使用 `callmanager_mini.gni`，并在注释中记录不适用的安全选项）和 `bundle.json`（部件 `call_manager_mini`、子系统 `telephony`、`adapted_system_type` 为 `["mini"]`、`destPath` 为 `base/telephony/call_manager_mini`，依赖 samgr_lite、kernel liteos_m、bounds_checking_function、init，Mini 构建参数登记到 `features`）。验证：`bundle.json` 通过 JSON 校验；每个 Mini 构建参数同时出现在 gni 的 `declare_args()` 和 `bundle.json` 中（由 1.7 的脚本检查）；telephony_call_manager 仓的 `git status` 为空（依赖目标名取自上游 BUILD.gn：`//foundation/systemabilitymgr/samgr_lite/samgr:samgr`、`//base/startup/init/interfaces/innerkits:libbegetutil`、`//third_party/bounds_checking_function:libsec_static`；另在临时 GN 根下以桩模板执行 `gn gen`，确认 38 个源文件、defines 与依赖连通。未在真实 OHOS lite 产品中构建）
- [x] 7.2 新增 `test/mini/tools/check_mini_rules.sh`，确认以下几点：telephony_call_manager 仓的 `git status` 为空；`vendor_files.py check` 通过（拷贝文件未被改动，其中不出现 `TELEPHONY_MINI_`）；Mini 代码中不出现 `try`/`catch`/`dynamic_cast`/`std::mutex`/`std::thread`/`std::condition_variable`（POSIX 适配除外）；Mini 日志调用中不带号码字段。验证：脚本在当前工作区通过；人为在某个拷贝文件中加入 `TELEPHONY_MINI_SYSTEM` 时，脚本报错（同时承担 7.5 的选项核对；另一负向用例：日志参数带 `phoneNum.c_str()` 时报错）
- [x] 7.3 新增 `test/mini/tools/check_auth_parity.py`：从标准仓 checkout（默认 `../telephony_call_manager`，可用参数指定）的 `call_manager_service.cpp` 中提取 design D6 表内各入口的系统应用判定与权限常量，并与 Mini `CallManagerService` 中的同一组入口比对。验证：脚本通过；人为删去 Mini 某个入口的一个权限判定时，脚本报错（只统计 `CheckPermission(...)` 中的权限常量：标准 `DialCall` 还把权限常量传给 HiSysEvent 故障记录）
- [x] 7.4 新增 `test/mini/tools/check_mini_symbols.sh`：对 CMSIS 门禁产物执行 `nm -C`，确认不含裁剪模块、IPC Stub/Proxy、HiSysEvent/HiTrace/dump 相关符号。验证：脚本通过；人为链接一个包含 `CallManagerServiceStub` 符号的目标文件时，脚本报错
- [x] 7.5 核对并记录安全编译选项：确认 gni 和两个门禁脚本中生效的 `-fno-exceptions -fno-rtti -fstack-protector-all -Wall -Wextra -Werror`；在 design D12 和 `BUILD.gn` 注释中记录 `_FORTIFY_SOURCE`、CFI、PAC 的差异和风险。验证：三处构建描述中都能查到这些选项；每个不适用的选项都有对应记录
- [ ] 7.6 （需目标板）在目标板上测量主循环任务的栈水位，覆盖来电、拨号失败、批量上报等最深调用链，据此定稿 `CALL_MANAGER_MINI_TASK_STACK_SIZE`。验证：实测峰值和余量回填到 design 常量表
- [ ] 7.7 （需目标板）在最小配置下测量 Mini 产物的 ROM（map 文件）和通话过程中的堆峰值。验证：测量结果回填到 design；超出产品预算时，另起裁剪变更，并在 design 的“Open Questions”中登记
- [x] 7.8 连续两次运行 `mini_selftest.sh` 和 `run_mini_cmsis.sh`，并逐项执行 `AGENTS.md` 的 DoD、`docs/agents/build-and-test.md` 中适用的静态自检和 `AGENTS_71.md` 第 7 节的完成检查清单；汇总“需人工复核”项（D9 信任模型、D2 移植复用、D10 音频降级、新部件的 `bundle.json`、拷贝文件与标准仓的同步策略）。验证：两个门禁两次都全部通过；检查记录中每一项都标明“已验证”或“未验证及原因”（记录见 `verification.md`）
